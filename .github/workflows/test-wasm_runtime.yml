name: Test `wasm_runtime.so`

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build-windows:
    runs-on: windows-latest
    env:
      APACHE_PATH: apache_httpd
    steps:
      - uses: actions/checkout@v3

      - name: Setup MSVC for C/C++ development
        uses: ilammy/msvc-dev-cmd@v1

      - name: Checkout Apache HTTP Server repository into ${{ env.APACHE_PATH }}
        uses: actions/checkout@v3
        with:
          repository: apache/httpd
          path: ${{ env.APACHE_PATH }}

      - name: Checkout Apache Portable Runtime (APR) into ${{ env.APACHE_PATH }}/srclib/apr
        run: cd ${{ env.APACHE_PATH }} && svn co http://svn.apache.org/repos/asf/apr/apr/trunk srclib/apr

      - name: DIR
        run: dir
      - name: echo
        run: echo ${{ env.APACHE_PATH }}
      - name: DIR ${{ env.APACHE_PATH }}
        run: dir ${{ env.APACHE_PATH }}

      # - name: Compile mod_wasm.so \
      #   run: cl.exe /nologo /MD /O2 /LD /W3 -DWIN32 -D_WIN32 \
      #     -I%APACHE_PATH%\include -I..\wasm_runtime\include \
      #     /c /Fomod_wasm.obj mod_wasm\modules\wasm\mod_wasm.c

      - name: Compile mod_wasm.so      
        run: cl.exe /nologo /MD /O2 /LD /W3 -DWIN32 -D_WIN32
            -I./${{ env.APACHE_PATH }}/include -I../wasm_runtime/include
            /c /Fomod_wasm.obj mod_wasm/modules/wasm/mod_wasm.c

      
# jobs:
#   test:
#     strategy:
#       fail-fast: false
#       matrix:
#         os: [ubuntu-latest, windows-latest, macos-latest]
#     runs-on: ${{ matrix.os }}
#     steps:
#     - uses: actions/checkout@v3
#     - name: Build
#       working-directory: ./wasm_runtime
#       run: cargo build --verbose
#     - name: Run tests
#       working-directory: ./wasm_runtime
#       run: cargo test --verbose
